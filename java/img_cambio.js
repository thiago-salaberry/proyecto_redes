//mobs pasivos
function cambiarImagenAjolote(nombre) {
    const imagen = document.getElementById('imagen-ajolote');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenAldeano(nombre) {
    const imagen = document.getElementById('imagen-aldeano');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenArmadillo(nombre) {
    const imagen = document.getElementById('imagen-armadillo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenBurro(nombre) {
    const imagen = document.getElementById('imagen-burro');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenCaballo(nombre) {
    const imagen = document.getElementById('imagen-caballo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenCaballoEsqueleto(nombre) {
    const imagen = document.getElementById('imagen-caballo-esqueleto');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenCamello(nombre) {
    const imagen = document.getElementById('imagen-camello');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenCerdo(nombre) {
    const imagen = document.getElementById('imagen-cerdo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenChampivaca(nombre) {
    const imagen = document.getElementById('imagen-champivaca');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenConejo(nombre) {
    const imagen = document.getElementById('imagen-conejo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenGato(nombre) {
    const imagen = document.getElementById('imagen-gato');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenGolemDeNieve(nombre) {
    const imagen = document.getElementById('imagen-golem-de-nieve');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenStrider(nombre) {
    const imagen = document.getElementById('imagen-strider');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenLoro(nombre) {
    const imagen = document.getElementById('imagen-loro');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenMula(nombre) {
    const imagen = document.getElementById('imagen-mula');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenMurcielago(nombre) {
    const imagen = document.getElementById('imagen-murcielago');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenOcelote(nombre) {
    const imagen = document.getElementById('imagen-ocelote');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenOveja(nombre) {
    const imagen = document.getElementById('imagen-oveja');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenPezGlobo(nombre) {
    const imagen = document.getElementById('imagen-pez-globo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenPollo(nombre) {
    const imagen = document.getElementById('imagen-pollo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenRana(nombre) {
    const imagen = document.getElementById('imagen-rana');
    imagen.src = `../imagenes/${nombre}.png`;
}
//mobs neutrales
function cambiarImagenCabra(nombre) {
    const imagen = document.getElementById('imagen-cabra');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenVaca(nombre) {
    const imagen = document.getElementById('imagen-vaca');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenEnderman(nombre) {
    const imagen = document.getElementById('imagen-enderman');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenGolemHierro(nombre) {
    const imagen = document.getElementById('imagen-golem-hierro');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenLlama(nombre) {
    const imagen = document.getElementById('imagen-llama');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenLobo(nombre) {
    const imagen = document.getElementById('imagen-lobo');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenOsoPolar(nombre) {
    const imagen = document.getElementById('imagen-oso-polar');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenPanda(nombre) {
    const imagen = document.getElementById('imagen-panda');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenPiglin(nombre) {
    const imagen = document.getElementById('imagen-piglin');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenPiglinZombificado(nombre) {
    const imagen = document.getElementById('imagen-piglin-zombificado');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenZorro(nombre) {
    const imagen = document.getElementById('imagen-zorro');
    imagen.src = `../imagenes/${nombre}.png`;
}
//mobs hostiles
function cambiarImagenAldeanoZombi(nombre) {
    const imagen = document.getElementById('imagen-aldeano-zombi');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenBlaze(nombre) {
    const imagen = document.getElementById('imagen-blaze');
    imagen.src = `../imagenes/${nombre}.gif`;
}

function cambiarImagenBruja(nombre) {
    const imagen = document.getElementById('imagen-bruja');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenCienago(nombre) {
    const imagen = document.getElementById('imagen-cienago');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenCreeper(nombre) {
    const imagen = document.getElementById('imagen-creeper');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenCuboDeMagma(nombre) {
    const imagen = document.getElementById('imagen-cubo-de-magma');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenDisecado(nombre) {
    const imagen = document.getElementById('imagen-disecado');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenErrante(nombre) {
    const imagen = document.getElementById('imagen-errante');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenEsqueleto(nombre) {
    const imagen = document.getElementById('imagen-esqueleto');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenEsqueletoDelWither(nombre) {
    const imagen = document.getElementById('imagen-esqueleto-del-wither');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenGhast(nombre) {
    const imagen = document.getElementById('imagen-ghast');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenGuardian(nombre) {
    const imagen = document.getElementById('imagen-guardian');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}
function cambiarImagenGuardianAnciano(nombre) {
    const imagen = document.getElementById('imagen-guardian-anciano');
    if (nombre.endsWith('.gif')) {
        imagen.src = `../imagenes/${nombre}`;
    } else {
        imagen.src = `../imagenes/${nombre}.png`;
    }
}

function cambiarImagenHoglin(nombre) {
    const imagen = document.getElementById('imagen-hoglin');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenInvocador(nombre) {
    const imagen = document.getElementById('imagen-invocador');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenPiglinBruto(nombre) {
    const imagen = document.getElementById('imagen-piglin-bruto');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenSaqueador(nombre) {
    const imagen = document.getElementById('imagen-saqueador');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenShulker(nombre) {
    const imagen = document.getElementById('imagen-shulker');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenVex(nombre) {
    const imagen = document.getElementById('imagen-vex');
    imagen.src = `../imagenes/${nombre}.gif`;
}

function cambiarImagenVindicador(nombre) {
    const imagen = document.getElementById('imagen-vindicador');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenZoglin(nombre) {
    const imagen = document.getElementById('imagen-zoglin');
    imagen.src = `../imagenes/${nombre}.png`;
}

function cambiarImagenZombi(nombre) {
    const imagen = document.getElementById('imagen-zombi');
    imagen.src = `../imagenes/${nombre}.png`;
}


